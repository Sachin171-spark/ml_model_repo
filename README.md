# ml_model_repo
Notebook includes: 3-layer Neural Network (NumPy), Naive Bayes (Manual &amp; Sklearn), Bayesian Network (pgmpy), Clustering (K-Means vs EM), KNN (Iris dataset), and Locally Weighted Regression (Bokeh). Covers classification, regression, and clustering with performance metrics.
